# v0.3.0 direct release — 2026-09-02

## Authorization and boundary

The user explicitly authorized skipping the repeated release preflight and directly publishing `v0.3.0`. The authorization was interpreted narrowly: preserve `scripts/release.sh` built-in safety checks, create and publish the immutable stable release, but do not connect to or change production and do not delete `nightly`.

Production recovery preparation, production smoke/observation, and the success-gated nightly cleanup remain pending. The release-preflight waiver does not waive those gates.

## Release execution

- Pre-execution state: clean `main`; local `HEAD == origin/main == ac03b7ff4d0aea4507407c15c3782f65e14ec066`; no local/remote/GitHub `v0.3.0` tag or release.
- Executed `scripts/release.sh v0.3.0` and answered its normal push prompt affirmatively.
- The script created release commit `8f211a811133b10868e6977854fbef52f900d056` (`chore: release v0.3.0`) and changed only `CHANGELOG.md` and `frontend/package.json`.
- The script created immutable tag `v0.3.0` at that commit and normally pushed `main` plus the new tag. No force push, tag move, or tag reuse occurred.

## Publication result

- Tag-triggered Release workflow `33584926291` completed successfully: <https://github.com/ming-kang/BUPT_EC/actions/runs/33584926291>.
- GitHub published <https://github.com/ming-kang/BUPT_EC/releases/tag/v0.3.0> at `2026-09-02T02:56:06Z` as a non-draft, non-prerelease stable release and marked it Latest.
- GitHub listed exactly the expected top-level asset names: `bupt-ec-linux-amd64.tar.gz`, `bupt-ec-linux-arm64.tar.gz`, `checksums.txt`, and `install.sh`.
- `v0.2.0` is no longer Latest. The `nightly` prerelease and tag remain unchanged.

The user-authorized skip means release notes byte comparison, protected-directory asset download/checksum/layout verification, Installer parity, binary version inspection, and stable URL download checks were not repeated here. Those checks remain required by the task plan before any production upgrade.

## Non-blocking workflow annotations

The successful workflow emitted GitHub-hosted cache service warnings and Node.js action-runtime deprecation notices. No job or release publication step failed.
