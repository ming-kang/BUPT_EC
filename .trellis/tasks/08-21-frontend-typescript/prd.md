# Frontend TypeScript migration (parent)

## Goal

F-01 (+F-02 later): staged TypeScript migration of frontend/src. Parent task owning requirement set, child task map, and cross-child acceptance. Children to be created during planning: type generation, core modules, component layer, prop-types removal; React 19 upgrade at the end.

## Requirements

- TBD

## Acceptance Criteria

- [ ] TBD

## Notes

- Keep `prd.md` focused on requirements, constraints, and acceptance criteria.
- Lightweight tasks can remain PRD-only.
- For complex tasks, add `design.md` for technical design and `implement.md` for execution planning before `task.py start`.
